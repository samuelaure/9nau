# Root .env for Docker Compose and general project settings
# This file is a template. Copy it to .env and fill in your actual values.

NODE_ENV=development
POSTGRES_USER=devuser
POSTGRES_PASSWORD=devpass
POSTGRES_DB=appdb
POSTGRES_PORT=5432
CYPRESS_BASE_URL=http://localhost:3001 # Base URL for Cypress E2E tests

# Test Database variables for Docker Compose
POSTGRES_TEST_PORT=5433
POSTGRES_TEST_USER=testuser
POSTGRES_TEST_PASSWORD=testpass
POSTGRES_TEST_DB=testdb
